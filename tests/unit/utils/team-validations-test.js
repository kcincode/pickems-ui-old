import teamValidations from 'pickems/utils/team-validations';
import { module, test } from 'qunit';

module('Unit | Utility | team validations');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = teamValidations();
  assert.ok(result);
});
