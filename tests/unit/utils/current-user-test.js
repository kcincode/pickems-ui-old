import currentUser from 'pickems/utils/current-user';
import { module, test } from 'qunit';

module('Unit | Utility | current user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentUser();
  assert.ok(result);
});
