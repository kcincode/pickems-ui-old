---
language: node_js
node_js:
  - "4"

sudo: false

env:
  - SURGE_LOGIN=nick.felicelli@gmail.com
  - SURGE_TOKEN=bb46b98b194e39d8df028cbe25213968

cache:
  directories:
    - node_modules

before_install:
  - npm config set spin false
  - npm install -g bower
  - bower --version
  - npm install phantomjs-prebuilt
  - phantomjs --version

install:
  - npm install
  - bower install

script:
  - npm test

before_deploy:
  - ember build -prod

deploy:
  skip_cleanup: true
  provider: surge
  project: ./dist

